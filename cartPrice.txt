CREATE DEFINER=`root`@`localhost` PROCEDURE `cartPrice`()
BEGIN
		SELECT SUM(der.price_p) FROM (
        SELECT p.price AS price_p
        FROM app_product p, app_select s
        WHERE s.product_id_id = p.product_id
        ) AS der;
END